SELECT 
    A.CATEGORY
    , SUM(B.SALES) AS TOTAL_SALES
FROM BOOK A
JOIN BOOK_SALES B
    ON B.BOOK_ID = A.BOOK_ID
    AND TO_CHAR(B.SALES_DATE, 'YYMMDD') BETWEEN '220101' AND '220131'
GROUP BY A.CATEGORY
ORDER BY A.CATEGORY ASC
;
